syntax = "proto3";
package profile;
option go_package = "";

message ProfileMessage {
  uint32 userID = 1;
  string address = 2;
}

service ReadProfile {
  rpc Profile(ProfileMessage) returns (ProfileMessage) {}
}